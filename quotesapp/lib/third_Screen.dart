import 'package:flutter/material.dart';
import 'package:quotesapp/third_Screen.dart';

class thirdscreen extends StatelessWidget {
  const thirdscreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}